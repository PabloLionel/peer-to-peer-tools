import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class UserService {
  // findOne(where?: any) {
  // }

  // find(where?: any) {
  // }

  // create(user: any /* User */) {
  // }

  // update(
  //   id: any /* typeof User.prototype.id */,
  //   change: any /* Partial<User> */
  // ) {
  // }

  // delete(id: any /* typeof User.prototype.id */) {
  // }
}
