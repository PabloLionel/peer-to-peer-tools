import { HideForProductionDirective } from './hide-for-production.directive';

describe('HideForProductionDirective', () => {
  it('should create an instance', () => {
    const directive = new HideForProductionDirective();
    expect(directive).toBeTruthy();
  });
});
